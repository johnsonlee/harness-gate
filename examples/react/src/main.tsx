import React from "react";
import { createRoot } from "react-dom/client";

createRoot(document.getElementById("root")!).render(
  <main><h1>Harness React</h1><p>This application passed the shared architecture check.</p></main>,
);
